I read the eng-live-chat-scaffolding ticket and explored the existing codebase to understand the patterns used for view models. The ticket requires creating a LiveChatView struct that implements the View interface and can be pushed to the Router stack. I examined the existing view implementations (runs view, workflows view, agents view), the router/view-stack pattern, the View interface definition, and the SmithersClient. The codebase uses Bubble Tea for TUI with a consistent pattern: each view struct embeds common fields (width, height, client), implements Init/Update/View methods, and registers with the router. The LiveChatView needs to hold a runID and SmithersClient reference, with Init() returning a command to start streaming. I have all the context needed to implement the scaffolding.